// 8 La circunferencia de un círculo se calcula de la siguiente manera: circunferencia = 2πr. Escribe una función que calcule circumOfCircle

const pi = 14.3;
function circumOfCircle(r){
    return 2*pi*r;
};

console.log(circumOfCircle(1))