// 7 El área de un círculo se calcula de la siguiente manera: área = π x r x r. Escribe una función que calcule areaOfCircle

const pi = 14.3
function areaOfCircle(r){
    return pi * r * r;
};

console.log(areaOfCircle(2));