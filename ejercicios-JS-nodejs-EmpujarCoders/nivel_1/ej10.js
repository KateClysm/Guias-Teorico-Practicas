// 10 La velocidad se calcula dividiendo la distancia total recorrida por un objeto en movimiento entre el tiempo total empleado. Escribe una función que calcule la velocidad de un objeto en movimiento, la velocidad.

function velocidad(distanciaTotal, tiempoTotal){
    return distanciaTotal / tiempoTotal;
};

console.log(velocidad(10,5));